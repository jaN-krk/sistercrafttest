import {assert} from './security';
export async function fetchOfficialRate(){const response=await fetch('https://www.tcmb.gov.tr/kurlar/today.xml',{signal:AbortSignal.timeout(10000)});assert(response.ok,502,'TCMB kuru şu anda alınamıyor. Son doğrulanan kur korundu.');const xml=await response.text();assert(xml.length<100000,502,'Kur yanıtı beklenenden büyük.');const date=xml.match(/Tarih="(\d{2})\.(\d{2})\.(\d{4})"/),usd=xml.match(/<Currency\s[^>]*Kod="USD"[\s\S]*?<\/Currency>/)?.[0];const rate=Number(usd?.match(/<ForexSelling>([\d.]+)<\/ForexSelling>/)?.[1]);assert(date&&Number.isFinite(rate)&&rate>1&&rate<100000,502,'TCMB kur bilgisi doğrulanamadı.');const rateDate=`${date[3]}-${date[2]}-${date[1]}`;assert(Date.parse(rateDate)<=Date.now()+86400000,502,'Kur tarihi doğrulanamadı.');return{tryPerUsd:rate,rateDate,source:'TCMB ForexSelling',sourceUrl:'https://www.tcmb.gov.tr/kurlar/today.xml',fetchedAt:new Date().toISOString()};}
